Task Manager 

A simple task management application built, designed to demonstrate clean backend logic, database operations, and automated testing using pytest.

---

🚀 Project Overview

This Task Manager allows users to:

* Add tasks
* View all tasks
* Update task details
* Mark tasks as complete
* Delete tasks

---


🧩 Key Features

Database Layer

* `create_table()` – Creates the tasks table if it doesn’t exist
* `add_task()` – Adds a new task
* `get_tasks()` – Retrieves all tasks
* `update_task()` – Updates task title and description
* `update_task_status()` – Updates task status
* `complete_task()` – Marks a task as complete
* `delete_task()` – Deletes a task

---

✅ Testing

* Tasks are added correctly
* Tasks can be marked as complete
* Tasks can be updated
* Tasks can be deleted

✅ How to run tests:

python3 -m pytest -v


---


👤 Author

Ambesa Mcwabane
Aspiring Software Developer | WeThinkCode_ Graduate

---


